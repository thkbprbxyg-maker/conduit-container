# Postgres (example values - change for real use)
POSTGRES_USER=conduit
POSTGRES_PASSWORD=conduit_password
POSTGRES_DB=conduit

# Backend
SECRET_KEY=change_me
PORT=8000
GUNICORN_APP_MODULE=conduit.wsgi:application

# Optional: if your backend uses a single DATABASE_URL string
